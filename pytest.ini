[pytest]
testpaths = tests
pythonpath = .
addopts = -v --tb=short -x
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
timeout = 300
markers =
    unit: testes unitários
    e2e: testes end-to-end (browser)
    slow: testes lentos
